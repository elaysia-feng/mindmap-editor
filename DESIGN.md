# InkMap Design System

> 2026-09-18 更新：当前实现采用「星图观测台」。下方 Alphabet Storm 为历史说明；新页面以本节和 `src/styles/styles.css` 的 Observatory Atlas 区段为准。

## 当前设计：星图观测台

中央画布承载结构，导航、文字稿和节点工具按需出现；本地文档库提供搜索、复制、JSON 备份、回收站与恢复。

- 夜间画布 `#081828`、面板 `#0d1d2c`、文字 `#f2f4f3`、主动作 `#e3ad59`。
- 日间画布 `#edf3f6`、文字 `#102333`、主动作 `#a96f20`。
- 中文系统字体；正文 14px、文档标题 16px、辅助文字 12px。按钮文案保持横向。
- 桌面顶部栏 68px；700px 以下为 60px 顶栏与 58px 底部导航。结构导航在移动端使用底部面板。
- 聚焦星路强调选中节点、祖先和直接子节点；大图保留阅读尺寸，外围分支通过拖拽浏览。
- 文档切换前提交编辑并等待保存；IndexedDB 写入以事务完成为准。
- 画布尺寸切换不做几何过渡，防止适配视图读取到中间尺寸。

功能回归和截图已验证；设计稿逐区域保真度门禁尚未完成，不将其记为视觉验收通过。

## 历史设计说明（已被上方规则替代）

## Direction contract

### THESIS

让文字成为可操作的材料：节点是被语义凝聚出来的词块，画布负责呈现它们的空间关系，Markdown 负责提供可快速重排的线性入口。

### OWN-WORLD

Alphabet Storm 是一套克制的纸面工作台。纸白承载内容，墨黑承载结构，银灰承载辅助信息，信号青绿只用于当前焦点、可执行动作和同步状态。文字可以被拆分、连接、折叠和恢复，但不会变成纯装饰。

### PRODUCT MECHANISM

同一份本地脑图可以在空间画布与 Markdown 文本之间来回变形，节点、层级、工作流连线和历史记录保持一致。

### FIRST VIEWPORT

左侧是窄而稳定的工作台导航；顶部集中显示产品身份、当前文档、本地保存状态、历史操作、视图切换和主要编辑动作；中央是带点阵参照的画布；右侧是总览、大纲和快捷键分区；底部是缩放、节点数、连线数和当前操作提示。

### STORY

用户先看到当前文档和结构状态，再在画布上操作。选中节点后，底部工具条承接高频动作；右侧导航负责总览与定位；命令面板把所有操作压缩成可搜索的入口；欢迎页提供空白入口和真实的 Markdown 示例。

### FORM

Operate 模式的语义图谱工作台。Vue 3 + TypeScript + Vite 只负责应用壳、导航和辅助页面；src/editor/controller.ts 保留画布高频交互和数据模型，不改变本地优先、可恢复和连线 DAG 约束。

## Surface language

### Palette and material

- 纸面：#f5f3ed / #fbfaf6，用于应用背景、面板和节点内容。
- 画布：#ecebe4，使用点阵作为空间参照；点阵只出现于画布和缩略图。
- 墨色：#202724，用于根节点、主文字和稳定导航。
- 银色：#9ca8a3，用于辅助文字、边界、折叠计数和结构参照。
- 信号青绿：#426f68，只用于选中、可执行主动作、同步状态和工作流连线。
- 深色主题从真实使用场景出发，保持纸面层级与信号对比，不用发光或渐变制造层次。

### Typography

- UI 和中文使用 Segoe UI Variable Text / Noto Sans SC / PingFang SC / Microsoft YaHei 系统栈。
- Markdown、快捷键和测量值使用 SF Mono / Cascadia Mono / Consolas。
- 标题用重量和字距表达层级；不使用渐变文字、装饰性大写标签来代替信息。

### Components

- 顶栏和右侧导航是有边界的实体表面，不使用玻璃模糊。
- 根节点是墨色实心词块；子节点使用纸面层级、边界和连接深度表达结构，不依赖彩虹色边框。
- 工作流连线是青绿色虚线，选中后变为实线；树关系使用稳定的灰墨线。
- 命令面板、帮助页、右键菜单、节点工具条和输入弹层共享边界、焦点环和阴影语言。
- 状态用“本地已保存 / 等待保存 / 解析失败”等真实文案表达，不把静态数值包装成虚假的进度指标。

## Interaction contract

- Tab 添加子节点，Enter 添加同级节点，F2 编辑，Delete 删除，Space 折叠或展开。
- F 适配可读视图，滚轮缩放，Space + 拖拽平移，Ctrl B 切换导航栏，Ctrl K 打开命令面板。
- 从输出端口拖到输入端口创建有向工作流连线；拖到空白处可先创建新节点再接线。
- 画布、Markdown 和分屏是同一份内容的三种视图，不是互相独立的页面。
- 错误、未选择、空白脑图、Markdown 解析提示和保存状态都必须有可见恢复路径。

## Responsive behavior

- 桌面保留左侧工作台导航、中央画布和右侧结构导航。
- 1180px 以下右侧导航转为按需打开的抽屉，画布扩大到剩余区域。
- 700px 以下左侧导航转为底部导航，顶部只保留身份、视图切换、搜索、主题和主要动作；分屏视图自动退化为 Markdown。
- Markdown 在窄屏占满可用工作区，编辑区保持行号、等宽字体、滚动同步和可见焦点。
- 所有动效在 prefers-reduced-motion: reduce 下关闭；所有按钮和输入保持键盘焦点。

## Decision notes

这次重设计吸收了几组不同材料的工作纪律，但只保留能服务产品事实的部分：从折叠纸面得到“状态可恢复”，从操作台得到“状态读数”，从等宽字符场景得到“文本是工作工具”。没有把磁带、赛车或折纸的外形直接套到界面上，避免让隐喻压过脑图与 Markdown 的清晰度。

## Finish criteria

- 首屏在三秒内能找到“新建节点、视图切换、搜索操作、右侧导航和本地保存状态”。
- 真实节点、选中、编辑、折叠、连线、Markdown 解析错误和空状态都具有清晰反馈。
- 桌面与窄屏均没有重叠、不可访问的控件或依赖颜色才能理解的状态。
- 完成构建、类型检查、Impeccable detector 和至少一轮桌面/移动视觉验收。
