# 更新日志

## [1.2.0] - 2026-09-19

### 新增
- 本地多文档库：搜索、复制、JSON 备份、回收站与恢复
- 使用 IndexedDB 保存文档，兼容迁移旧版 localStorage 脑图
- 星图观测台界面、按需导航、命令搜索与移动端底部操作栏

### 修复与改进
- 切换文档前提交编辑并等待保存，避免跨文档写入
- 等待 IndexedDB 事务完成后才确认写入完成
- 修复工具栏布局冲突，改善节点可读性与空白文档定位
- 保留 Markdown 双向同步、拖拽、撤销重做和工作流连线

### 使用说明
- 文档只保存在当前浏览器，建议定期导出 JSON 备份
- 请使用固定的 HTTP 地址和端口访问，以继续使用原有浏览器数据
- 设计稿逐区域视觉对齐尚未完成验收

## [1.1.0] - 2026-09-05

### 改进
- 迁移到 Vue 3 + TypeScript + Vite 工程结构
- 将工具栏、画布、侧栏、Markdown 面板和浮层拆分为 Vue 组件
- 保留原有脑图编辑核心、数据格式和本地存储兼容性
- Markdown 输入自动将普通文本整理为根标题或列表节点，并保留编辑光标
- 修复 Markdown 列表回写为多级标题的问题
- 优化脑图为 XMind 风格的左右双向布局，减少长竖线和节点重叠

## [1.0.1] - 2026-08-30

### 改进
- Release zip 现在附 `SHA256SUMS.txt`，可对下载产物做 SHA-256 完整性校验
- 工作流改为完全参数化：版本号 / tag / 文件名 / 链接统一从 `github.ref_name` 与 `steps.ver.outputs.version` 动态生成，后续发版零修改

### 兼容性
- 与 1.0.0 完全兼容，无需升级现有数据

## [1.0.0] - 2026-08-30

首个正式版本。

### 能力
- 脑图 / Markdown / 分屏三种视图常驻编辑
- 双击编辑、拖拽移动、滚轮缩放、空白处平移
- 撤销 / 重做（支持快捷键与按钮）
- 自动保存到 `localStorage`（schema 版本 v2，向前兼容 v1）
- Markdown 与脑图双向同步（含行号 / 节点互相定位）
- 文件操作：打开 / 导出 Markdown、JSON 备份与恢复
- 主题切换（浅色 / 深色，自动记忆）
- 缩略图、大纲、折叠 / 展开
- 工作流连线（DAG 约束，支持挂到既有节点或空白处新建）
- 层级整理：一键布局，`Shift + 拖拽` 改变父子关系

### 兼容性
- 数据向前兼容：可读取旧的 `mindmap-studio`（v1）数据
- 浏览器：Chrome / Edge / Firefox 最新版
